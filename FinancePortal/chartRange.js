function getId(monId)
{
id=monId.id;

// et on fait ce que l'on veux de l'id
return id;
}

function geTicker(Ticker){
    Ticker=Ticker.id;

// et on fait ce que l'on veux de l'id
return id;
}